# kaggle-state-farm-distracted-driver-detection

Entry in the Kaggle competition State Farm Distracted Driver Detection.

Uses a convolutional neural network to determine if a driver of a vehicle is distracted.
